@model IEnumerable<Shift_Scheduler.Models.ShiftSchedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employees.userName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dayOfTheWeek)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.shiftType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employees.userName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dayOfTheWeek)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shiftType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.shiftScheduleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.shiftScheduleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.shiftScheduleId })
        </td>
    </tr>
}

</table>
